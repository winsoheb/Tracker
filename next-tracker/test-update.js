const { updateScheduledTaskStatus } = require('./src/lib/actions/planner')
const { auth } = require('@/auth') // won't work in node

async function main() {
  console.log("Testing update...");
}
main();
